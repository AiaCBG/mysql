-- Consulta (SELECT) todos (*) los datos de (FROM) la tabla "users"
SELECT * FROM  users;

-- Consulta (SELECT) los nombres (name) de (FROM) la tabla "users"
SELECT name FROM  users;

-- Consulta (SELECT) las edades (age) de (FROM) la tabla "users"
SELECT age FROM  users;

-- Consulta (SELECT) los id de usuarios (user_id) y nombres (name) de (FROM) la tabla "users"
SELECT user_id, name FROM  users;