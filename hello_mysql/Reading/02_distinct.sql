-- Consulta (SELECT) todos (*) los datos distintos (DISTINCT) de (FROM) la table "users"
SELECT DISTINCT * FROM users;

-- Consulta (SELECT) las edsades (age) distintas (DISTINCT) de (FROM) la table "users"
SELECT DISTINCT age FROM users;