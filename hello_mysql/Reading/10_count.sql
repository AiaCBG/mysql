-- Consulta (SELECT) y cuenta (COUNT) todos (*) los datos de la tabla "users"
SELECT COUNT(*) FROM users;

-- Consulta (SELECT) y cuenta (COUNT) los datos que tienen edad (age) de la tabla "users"
SELECT COUNT(age) FROM users;
